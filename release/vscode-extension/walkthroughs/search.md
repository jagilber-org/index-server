## Search

The MCP server provides **keyword**, **regex**, and **semantic** search across your instruction index.

Open Copilot Chat in agent mode and try:

```
Search the index for security best practices
```

The server exposes `index_search` and `index_dispatch` tools that Copilot will call automatically.
