# .specify/commands — SpecKit command extensions
# Place custom command scripts here for repo-specific automation.
# This directory is intentionally sparse for index-server since
# the MCP server itself handles most automation via tool handlers.
